server:
  port: 8000
  servlet:
    context-path: /api/v1

feign:
  client:
    config:
      notifications-client:
        loggerLevel = full

spring:
  application:
    name: users-service
  jackson:
    serialization:
      indent-output: false
  profiles:
    include:
      - db
      - app
      - data